# Folder Structure

- `src/app` for routes and metadata
- `src/components` for UI composition
- `src/lib` for data, helpers, and shared constants
- `src/server` for backend storage/auth/config adapters
- `prisma` for schema
- `docs` for product and implementation strategy
- `docs/assets` for reference images and visual samples
- `public` for logo and social assets
